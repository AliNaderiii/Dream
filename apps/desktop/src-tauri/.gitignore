/target/
/gen/schemas/

# Bundled CPython runtime — downloaded at `tauri build` time (never committed).
# Keep only the placeholder + README so `bundle.resources` stays resolvable.
/resources/python/*
!/resources/python/.gitkeep
!/resources/python/README.md
