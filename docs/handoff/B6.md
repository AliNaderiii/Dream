# B6 — Isolated HITL page reads

Propose a public http(s) URL. Nothing is fetched until Allow once.
YOLO, Chrome profiles, computer-use, localhost, and credentials in URLs refuse.
`read_page` stays the only fetcher; SSRF is unchanged.
