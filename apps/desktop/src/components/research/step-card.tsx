/**
 * Step card — retained for backward compatibility.
 *
 * The live trace now uses EventRow directly, deriving cards from P1 events.
 * This module is kept as a placeholder in case future UI work re-introduces
 * a dedicated step-card component.
 */

export {};
