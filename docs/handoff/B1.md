# B1 — Space bot roster

**Base:** `origin/main` at `v0.4.4` (`fb396ac`). New domain `dream/bots`.

Calls `SpaceService.get` only. Does not rewrite `space/store.py` internals.

## Gates

- `yolo=True` → security error
- model URLs refused
- instruction injection quarantined
- memory does not leak across bots
- RPC errors map to `invalid_params` (transport fuzz)
