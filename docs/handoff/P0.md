# P0 integration seam log

- **Decision:** bridge discovery is restricted to sorted, package-local
  `methods_<domain>.py` modules. Built-in handler names win unconditionally.
- **Decision:** tools are discovered only from immediate package domains and are
  activated with bridge startup; failures are logged and quarantined.
- **Decision:** frontend route and locale registries use Vite globs. Existing
  routes/resources are unchanged; extension route components stay lazy.
- **Blocker:** this checkout has neither Python dev tools (`pytest`, `ruff`) nor
  desktop `node_modules` (`tsc`), so full gates cannot execute here.
- **Attempted evidence:** `python -m pytest tests/test_bridge_methods.py -q`
  failed before collection: `No module named pytest`; `python -m ruff check ...`
  failed: `No module named ruff`; `npm --prefix apps/desktop run typecheck`
  failed: `tsc: not found`.
- **Milestone:** added contract tests for bridge/tool discovery and frontend
  registry invariants. Python syntax compilation completed; runtime test import
  remains blocked by the absent optional runtime dependency `authlib`.
