# Space bots

Named specialists on a Space. Each bot has a geometric avatar (teal / amber /
rose / slate / violet — not a copied Hermes blue), a catalog role, a local
model id, its own instruction, and isolated memory (`bot:<id>`).

YOLO and Always Allow are refused.
