# Browse (HITL)

Paste a public http or https link. Dream queues it. You Allow once or Deny.
Deny fetches nothing. There is no Always Allow, no YOLO, and no signed-in Chrome.
Local addresses and credentials inside the URL are refused.
