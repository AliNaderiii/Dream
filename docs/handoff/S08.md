# S08 — Docs and release handoff

**Status: Gate G8-docs met.** This session changed documentation only; runtime
behaviour is unchanged.

## What changed

- `README.md` now identifies Tauri as the product UI, states that the sidecar
  integration and desktop build exist, and documents projects, the Jalali
  scheduler, conversation tool cards and approvals, Telegram pairing, and the
  current Iranian-user path.
- `docs/user/quick-start.md` uses only repository commands and scripts. It
  distinguishes Windows first-run (`run.bat` + Ollama) from building the Tauri
  app, adds plan/usage/route and Telegram pairing, and records that the live
  Telegram smoke is owner-run.
- `docs/PRODUCT.md` now reflects the product after Wave 2 rather than the S00
  split-shell state. It retains honest local metering/privacy boundaries and
  publishes no paid IRR amounts.
- `CHANGELOG.md` Unreleased now lists the user-visible S01–S07 work: hardened
  commerce, Iranian data workflows, Telegram, the bridge/build, desktop plan
  state, projects/scheduler, and conversation UX.

## Documentation command verification

Executed from the repository root:

```text
python cli.py --demo             -> exit 0
python cli.py --plan             -> exit 0; local / IRR / price 0
python cli.py --usage            -> exit 0; no local ledger attached
python cli.py --route            -> exit 0; echo privacy sentence
python doctor.py                 -> exit 0
python -m dream.telegram --help  -> exit 0
```

The installed command names documented are defined in `pyproject.toml`:
`dream`, `dream-bridge`, and `dream-telegram`. Desktop commands documented in
the quick start and README were checked against `apps/desktop/package.json`:
`dev`, `tauri dev`, and `tauri build` exist. `git diff --check` passes.

## Acceptance

- [x] README, PRODUCT, quick start, and Unreleased changelog match S00–S07.
- [x] Tauri is presented as the product UI and as buildable/sidecar-connected.
- [x] Windows first-run is accurately described as `run.bat` + Ollama.
- [x] Plan, usage, route, Telegram pairing, projects, Jalali scheduler, tool
      cards, and approval dialog are documented.
- [x] Iranian-user section says Ollama, no VPN, unlimited local plan, and paid
      plans TBD after cost measurement.
- [x] No numeric paid IRR price or hosted-model URL was invented.
- [x] Runtime files were not changed.
