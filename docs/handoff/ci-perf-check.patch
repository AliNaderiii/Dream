diff --git a/.github/workflows/ci.yml b/.github/workflows/ci.yml
index 00c03c5..ec2602d 100644
--- a/.github/workflows/ci.yml
+++ b/.github/workflows/ci.yml
@@ -31,0 +32,3 @@ jobs:
+      # Keep pytest isolated from frontend/Rust builds in this job. Telegram
+      # timing assertions are sensitive to CPU contention and can flake when a
+      # developer runs a concurrent build; each CI job has its own runner.
diff --git a/.github/workflows/desktop-ci.yml b/.github/workflows/desktop-ci.yml
index 503325d..c63e952 100644
--- a/.github/workflows/desktop-ci.yml
+++ b/.github/workflows/desktop-ci.yml
@@ -8 +8 @@ on:
-    branches: [main, 'arena/**', 'feat/**', 'release/**']
+    branches: [main, "arena/**", "feat/**", "release/**"]
@@ -10,2 +10,2 @@ on:
-      - 'apps/desktop/**'
-      - '.github/workflows/desktop-ci.yml'
+      - "apps/desktop/**"
+      - ".github/workflows/desktop-ci.yml"
@@ -14,2 +14,2 @@ on:
-      - 'apps/desktop/**'
-      - '.github/workflows/desktop-ci.yml'
+      - "apps/desktop/**"
+      - ".github/workflows/desktop-ci.yml"
@@ -57,0 +58,18 @@ jobs:
+      - name: Build performance bundle
+        run: npm run build
+
+      - name: Performance budgets
+        run: npm run performance:check
+
+      - name: Accessibility and reduced-motion gates
+        run: npm run accessibility:check
+
+      - name: Upload performance report
+        if: always()
+        uses: actions/upload-artifact@v4
+        with:
+          name: desktop-performance-${{ github.sha }}
+          if-no-files-found: warn
+          retention-days: 14
+          path: apps/desktop/performance-results.json
+
